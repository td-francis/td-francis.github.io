/**
 * Modified from the original ninja-keys footer template
 * (@deepdub/ninja-keys, dist/ninja-footer.js).
 *
 * The original exported a keyboard-hint footer bar ("to select",
 * "to navigate", "to close", "move to parent"). This version exports an
 * empty template so ninja-keys.min.js (which imports and renders this
 * unconditionally) shows no footer at all.
 */
import { html } from "./lit/index.min.js";
export const footerHtml = html``;
